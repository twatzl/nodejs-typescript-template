{
  "compilerOptions": {
    "noImplicitAny": true,
    "target": "es5",
    "declaration": true,
    "sourceMap": true,
	"experimentalDecorators": true
  }
  // include, exclude, types and typeRoots options are not working with this configuration
  // please use gulpfile.config.js to configure these options
}